//> using scala "3.3"

// Almond kernel configuration settings
kernel.silent(true) // Suppress the debug outputs of the kernel

// Load any dependencies that are needed for the whole notebook

// import $ivy.`org.apache.spark::spark-sql:2.4.5` // Example

// Define any function that are needed for the whole notebook

// def someFunction(x: Int): Int = x + 1
